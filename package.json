{
  "name": "currency-desk-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Orchestration + workspace root only — frontend, backend, and the shared engine package are independent, each with its own package.json. See /frontend, /backend, /packages/engine.",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev:all": "concurrently -n web,api -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vitest": "^4.1.11"
  }
}
